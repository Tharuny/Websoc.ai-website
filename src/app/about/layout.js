export const metadata={
    title:"About us",
    description: "Websoc.ai is a mature IT services provider with years of experience in software development, Application Development, and Outsourced service provider..."
}
export default function Layout({children}){
    return(
        <main>
            {children}
        </main>
    )
}
export const metadata={
    title:"Products",
    description: "Websoc.ai has a different products like Hospital Management Software, Learning Management System, Omni Channel Chat, Charger Management System..."
}
export default function Layout({children}){
    return(
        <main>
            {children}
        </main>
    )
}
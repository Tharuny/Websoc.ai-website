export const metadata={
    title:"Portfolio - Websoc.ai",
    description: "Websoc.ai will deliver the best software development service, Digital Marketing service. We build the infrastructure of the robotic automation industry."
}
export default function Layout({children}){
    return(
        <main>
            {children}
        </main>
    )
}
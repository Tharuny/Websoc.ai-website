export const metadata={
    title:"Internship | Websoc.ai",
    description: "Best software training institutes in hyderabad FullStack + Devops Course,python,Java, Digital Marketing services..."
}
export default function Layout({children}){
    return(
        <main>
            {children}
        </main>
    )
}
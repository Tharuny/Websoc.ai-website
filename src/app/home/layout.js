export const metadata={
    title:"WEBSOC.AI",
    description: "Websoc.ai is the leading digital platform for all business firms across the world and an one stop automated solution and integrated with Artificial Intelligence.",
}
export default function Layout({children}){
    return(
        <main>
            {children}
        </main>
    )
}
export const metadata={
    title:"Services",
    description: "Websoc.ai will provide best Software Development services, Digital Marketing, Artificial Intelligence, CRM, HMS many services will be ..."
}
export default function Layout({children}){
    return(
        <main>
            {children}
        </main>
    )
}